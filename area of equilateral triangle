#include<stdio.h>
#include<math.h>
int main() 
{
   int side;
   float area, r;
   r=sqrt(3)/4;
   printf("\nEnter the Length of Side : ");
   scanf("%d", &side);
   area=r*side*side;
   printf("\nArea of Equilateral Triangle : %f", area);
   return (0);
}
