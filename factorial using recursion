#include<stdio.h>
long(factorial(int n))
{
	if (n==0)
	{
		return 1;
	}
	else
	{
		return(n*factorial(n-1));
	}
}
void main()
{
	int n;
	long c;
	printf("enter the number:");
	scanf("%d",&n);
	c=factorial(n);
	printf("the factorial of %d is %d. ",n,c);
}
